# Channel API — batch B

Source: JADX-decompiled Divoom Android app at
`references/apk/decompiled_src/sources/com/divoom/Divoom/` (gitignored, absolute-path only).
Master command catalog: `http/HttpCommand.java`. Request/response shapes: `http/request/**`,
`http/response/**` (`@JSONField(name = "...")`). Callers: `view/fragment/**`, `**/model/*.java`.

**Web coverage check** (one round of searches, per the source instructions): searched for
public documentation of `app.divoom-gz.com`/`appin.divoom-gz.com` covering any of these 50
commands. The closest thing to a community reverse-engineering doc is the unofficial
"REvoom Team" app-endpoint page (`https://divoom.2a03.party/api/app.html`), which documents a
handful of `Channel/*` endpoints (`EditAll`, `GetAll`, `GetConfig`, `GetCurrent`, `GetSongInfo`,
`GetVolume`, `SetConfig` (undocumented fields), `SetCurrent`, `SetEqData`, `SetFrame`,
`SetVolume`) and lists many more as bare, unexplored endpoint names — but **none of the 50
commands in this batch appear anywhere on that page**, not even in its "undocumented" bare-name
list. The official `doc.divoom-gz.com`/`docin.divoom-gz.com` docs and the `r12f/divoom` /
`govoom` / `divoom-tool` OSS clients all target the LAN/local Pixoo-64 REST API, not the
`app.divoom-gz.com` cloud/social API this batch belongs to. **No command in this batch is
web-confirmed** — every row below is sourced purely from the decompiled APK.

| Command | Purpose | Request fields | Response fields | Relevance | Confidence |
|---|---|---|---|---|---|
| `Channel/GetSubscribeGalleryTime` | Fetch the configured gallery-display-duration setting for the subscribe channel shown on the device screen. | none (generic `BaseChannelRequest`: `ClockId`, `PageIndex`, `ParentClockId`, `LcdIndependence`, `LcdIndex`, `Language`, `ParentItemId`) | `GalleryShowTimeFlag`, `SingleGalleyTime` (`WifiChannelGetSubscribeGalleryTimeResponse`) | device-control | decompiled |
| `Channel/GetSubscribeTime` | Fetch the scheduled on/off time window (start/end, weekday mask, voice-enable) for the subscribe channel display; shares response shape with GetEqTime/GetAlbumTime. | none (generic `BaseChannelRequest`) | `EndTime`, `IsEnable`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelTimeItemResponse`) | device-control | decompiled |
| `Channel/ItemSearch` | Search/browse gallery items within a channel category (clock-face store search by keyword). | `ClockId`, `EndNum`, `ItemFlag`, `ItemId`, `Key`, `Language`, `StartNum` (`WifiChannelItemSearchRequest`) | `SearchList` (list of items; `WifiChannelItemSearchResponse`) | device-control | decompiled |
| `Channel/LikeClock` | Like/unlike a clock face in the gallery. | `ClockId`, `LikeFlag` (`MyClockLikeClockRequest`) | none (generic `BaseResponseJson`, RC only) | account/social | decompiled |
| `Channel/LoginThird` | Log in / link a third-party account (DouYin, Weibo, Twitter, Facebook, Spotify, Strava, Fitbit, Google, Uber) to the Divoom cloud account. | `LogInfo` (List\<String\>), `Type` (+ `BaseChannelRequest` base) (`WifiChannelLoginThirdRequest`) | not found (no caller located beyond the request class; presumed generic RC-only, unverified) | account/social | decompiled |
| `Channel/MyClockAdd` | Add a clock face to the user's "My Clock" favorites list. | `ClockId` (`MyClockAddRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/MyClockBatchRemove` | Remove multiple clock faces from the "My Clock" list in one batch call. | `ClockList` (List\<Integer\>) (`MyClockBatchRemoveRequest`) | `ClockList` (list of `ClockListItem`; reuses `MyClockGetListResponse`) | device-control | decompiled |
| `Channel/MyClockGetList` | Fetch the user's "My Clock" (favorited clock faces) list. | `Flag` (+ `BaseLoadMoreRequest`: `EndNum`, `StartNum`, `Language`, `CountryISOCode`) (`MyClockGetListRequest`) | `ClockList` (list of `ClockListItem`) (`MyClockGetListResponse`) | device-control | decompiled |
| `Channel/MyClockInsertPosition` | Reorder a clock in the "My Clock" list by moving it next to another clock. | `ClockId`, `NewPositionClockId` (`MyClockInsertPositionRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/MyClockRemove` | Remove a single clock face from the "My Clock" list. | `ClockId` (`MyClockRemoveRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/NewPixelClock` | Create a new custom/DIY pixel-art clock face (e.g. from a photo-album flow) and register it server-side. | `ClockType` (+ `BaseChannelRequest` base) (`WifiChannelNewPixelClockRequest`) | `ClockId`, `ClockName`, `ClockType`, `ImagePixelId` (`WifiChannelNewPixelClockResponse`) | device-control | decompiled |
| `Channel/OnOffScreen` | Remotely turn the device's screen on/off (setter; distinct from the `GetOnOffScreen` getter). | `OnOff` (`ChannelOnOffScreenRequest`) | none (generic `BaseResponseJson`, RC only; `WifiChannelGetOnOffScreenResponse` belongs to the separate getter, not this command) | device-control | decompiled |
| `Channel/RenamePixelClock` | Rename a custom/DIY pixel clock face. | `ClockId`, `ClockName` (`WifiChannelRenamePixelClockRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/ReportClockComment` | Post/reply to a comment on a clock-face design in the cloud gallery (shares the `ReportCommentV2` request/response pair, posted under a different command string). | `AckUserId`, `AtList`, `ClockId`, `Comment`, `CommentId`, `ForumId`, `GalleryId` (`ReportCommentV2Request`) | `CommentId` (`ReportCommentV2Response`, extends generic RC response) | account/social | decompiled |
| `Channel/ResetClock` | Tell the device to reset/reload the currently-selected clock face's runtime data. | `ClockId` (+ `BaseChannelRequest` base) (`WifiChannelResetClockRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/Set5LcdChannelType` | Set which channel/mode type is shown on a given LCD panel of a multi-panel (5-LCD) device. | `ChannelType`, `LcdIndependence` (+ base) (`WifiChannelSet5LcdChannelTypeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/Set5LcdWholeClockId` | Apply one clock face across the entire 5-LCD panel array (vs. per-panel). | `ClockId` (+ base) (`WifiChannelSet5LcdWholeClockIdRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetAlbumTime` | Set the display schedule (start/end time, active weekdays, voice-alert flag, enabled flag) for the photo-album slideshow feature. | `EndTime`, `IsEnable`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelTimeItemRequest`, confirmed via caller) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetAllCustomTime` | Bulk-set display schedules for a list of custom/subscribed clock time slots at once. | `TimeList`: List\<`WifiChannelCustomTimeItem`\> each with `EndTime`, `IsEnable`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelSetAllCustomTimeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetAmbientLight` | Configure the device's ambient/RGB backlight: brightness, color, color-cycling, EQ-reactive effect selection. | `Brightness`, `Color`, `ColorCycle`, `EqOnOff`, `SelectEffect` (`WifiChannelSetAmbientLightRequest`, extends base `BaseRequestJson` — no channel/clock context fields) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetBrightness` | Set the device screen/display brightness level. | `Brightness` (+ base) (`WifiChannelSetBrightnessRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetClockConfig` | Push a clock face's widget/element configuration (two ordered item lists) for a given clock id to the device. | `ClockId`, `ItemList`, `ItemList2` (both List\<`SettingListItem`\>) (`WifiChannelSetClockConfigRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetClockSelectId` | Select/activate a specific clock face by id on the device. | `ClockId` (+ base) (`WifiChannelSetClockSelectIdRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetClockStyle` | Set the visual style/skin variant for a given clock face id. | `ClockId`, `StyleId` (+ base) (`WifiChannelSetClockStyleRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetConfig` | Configure device-wide channel/startup behavior: default channel index, gallery rotation/timing, LCD startup channel/array, startup clock id/image. | `ChannelIndex`, `ClockTime`, `GalleryShowTimeFlag`, `GalleryTime`, `LcdStartupArray`, `LcdStartupChannel`, `RotationFlag`, `SingleGalleyTime`, `StartUpClockId`, `StartUpClockImageFileId`, `StartUpClockOnOff` (`WifiChannelSetConfigRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetCustom` | Apply/save a custom clock-face design (pulled from the cloud gallery, with author attribution) into a device clock slot. | `AuthorHeadId`, `AuthorId`, `AuthorNickName`, `CustomId`, `CustomPageIndex`, `FileId`, `GalleryId`, `ImageName`, `SoundFileId` (`WifiChannelSetCustomRequest`) | `CustomId` (`WifiChannelSetCustomResponse`, extends generic RC response) | device-control | decompiled |
| `Channel/SetCustomGalleryTime` | Set per-clock-face custom gallery display duration/flags (how long a DIY/gallery page shows and whether sound plays). | `CustomId`, `GalleryShowTimeFlag`, `SingleGalleyTime`, `SoundOnOff` (+ base) (`WifiChannelSetCustomGalleryTimeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetCustomId` | Set the active custom/gallery item id for the current channel. | `CustomId` (+ base) (`WifiChannelSetCustomIdRequest`) | none (generic response; no dedicated class, no caller located to confirm) | device-control | decompiled |
| `Channel/SetCustomPageIndex` | Set which custom-channel page/index is currently active (paired with the `Channel/GetCustomPageIndex` read). | `CustomPageIndex` (+ base) (`WifiSetCustomPageIndexRequest`) | none (generic response; no dedicated Set-response class, no caller located to confirm) | device-control | decompiled |
| `Channel/SetEqPosition` | Set the on-screen position of the EQ (audio spectrum) visualization. | `EqPosition` (+ base) (`WifiChannelSetEqPositionRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetEqTime` | Set the EQ display's active time window/schedule (enable flag, start/end time, voice-reactive toggle, weekday schedule). | `EndTime`, `IsEnable`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelTimeItemRequest`, confirmed via caller) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetIndependenceConfig` | Set the "independence" (multi-LCD segment) mode schedule/config — enable flag, time window, voice toggle, weekday schedule. | `EndTime`, `IsEnable`, `LcdIndependence`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelSetIndependenceConfigRequest`) | `GroupList` (reuses `WifiGet5LcdClockListResponse` — the updated 5-LCD clock group list after the change) | device-control | decompiled |
| `Channel/SetIndependenceName` | Set the display name of an independent/multi-screen LCD segment. | `IndependenceName`, `LcdIndependence` (`WifiChannelSetIndependenceNameRequest`) | none (generic response; no dedicated class, no caller located to confirm) | device-control | decompiled |
| `Channel/SetIndex` | Select/switch the device's active channel index (clock / cloud dial / visualizer / custom). | `SelectIndex` (+ base) (`WifiChannelSetIndexRequest`) | none (generic response; no dedicated class, no caller located to confirm) | device-control | decompiled |
| `Channel/SetNightPreview` | Set the brightness used for a "night mode" preview. | `Brightness` (`WifiChannelSetNightPreviewRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetNightView` | Set the night-mode schedule: on/off, brightness, and active time window. | `Brightness`, `EndTime`, `OnOff`, `StartTime` (`WifiChannelSetNightViewRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetNotifyList` | Set the list of notification items (id/name/on-off) shown on the device's notification channel. | `NotifyList`: list of `{NotifyId, NotifyName, NotifyOnOff}` (`WifiChannelSetNotifyListRequest`) | `NotifyList` (reuses `WifiChannelGetNotifyListResponse`: `NotifyId`, `NotifyName`, `NotifyOnOff`) | device-control | decompiled |
| `Channel/SetOnOff` | Turn the channel/display on or off, optionally on a scheduled time window. | `EndTime`, `OnOff`, `StartTime` (`WifiChannelSetOnOffRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetProduceTime` | Push a "produce time" (schedule/production timestamp) value for the active channel/clock config to the cloud. | `ProduceTime` (+ base) (`WifiChannelSetProduceTimeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetRGBInfo` | Push RGB/light-strip settings (brightness, color, on/off, color-cycle mode, per-light list) for a channel to the cloud. | `Brightness`, `Color`, `ColorCycle`, `KeyOnOff`, `LightList`, `OnOff`, `SelectLightIndex` (`WifiChannelSetRGBInfoRequest`, extends base `BaseRequestJson`) | `Brightness`, `Color`, `ColorCycle`, `KeyOnOff`, `LightList`, `OnOff`, `SelectLightIndex` (reuses `WifiChannelGetRGBInfoResponse`) | device-control | decompiled |
| `Channel/SetStartupChannel` | Set which channel index the device boots to on startup. | `ChannelIndex` (+ base) (`WifiChannelSetStartupRequest`); note: no live caller of this command exists anywhere in the decompiled sources — matched purely by naming convention, appears unused in this app build | none found; no caller to confirm, sibling commands all use generic RC-only response | device-control | decompiled |
| `Channel/SetSubscribe` | Attach/subscribe a photo album or playlist (with selected users) to a channel slot on the device. | `AlbumId`, `AlbumName`, `AuthorType`, `PlayId`, `PlayName`, `SubscribeType`, `UserList` (list of `{SelectFlag, UserId}`) (`WifiChannelSetSubscribeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetSubscribeGalleryTime` | Set photo/gallery display timing (per-image duration, show-time flag) for a subscribed channel. | `GalleryShowTimeFlag`, `SingleGalleyTime` (+ base) (`WifiChannelSetSubscribeGalleryTimeRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/SetSubscribeTime` | Set a scheduled show/hide time window (start/end time, weekday array, voice-alert enable) for a subscribed clock-face channel. | `EndTime`, `IsEnable`, `StartTime`, `VoiceEnable`, `WeekArray` (`WifiChannelTimeItemRequest`, confirmed via caller — the `WifiChannelCustomTimeItem` DTO is only an intermediate UI model, not the wire request) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |
| `Channel/StoreClockGetClassify` | Fetch the category/classification list for browsing the online clock-face store. **Known live RC=12 (`HTTP_REQUEST_EMPTY`) issue, superseded by the public `Channel/GetDialType`+`Channel/GetDialList` endpoint** — not re-investigated per prior-session finding. | none (generic `BaseLoadMoreRequest`: `StartNum`, `EndNum`, `Language`, `CountryISOCode`) | `ClassifyList`: list of `MyClockStoreClockGetClassifyListItem` (`ClassifyId`, `ClassifyName`) | device-control | decompiled |
| `Channel/StoreClockGetList` | Fetch the paged clock-face list within a store category, for the online clock-face store. **Known live RC=12 (`HTTP_REQUEST_EMPTY`) issue, superseded by the public `Channel/GetDialType`+`Channel/GetDialList` endpoint** — not re-investigated per prior-session finding. | `ClassifyId`, `Flag` (+ `BaseLoadMoreRequest` base) (`MyClockStoreClockGetListRequest`) | `ClockList`: list of `ClockListItem` (`AddFlag`, `ClockExPlain`, `ClockExPlainPicId`, `ClockId`, `ClockName`, `ClockType`, `ConfigFlag`, `ImagePixelId`) (`MyClockStoreClockGetListResponse`) | device-control | decompiled |
| `Channel/StoreGetBanner` | Fetch promotional banner tiles (e.g. featured collections) shown atop the online clock-face store screen. | none (generic `BaseLoadMoreRequest`) | `BannerList`: list of `MyClockStoreBannerListItem` (`BannerImageId`, `BannerName`, `ClockList` [list of `ClockListItem`]) | device-control | decompiled |
| `Channel/StoreNew20` | Fetch the 20 newest clock faces in the online store; reuses the same request/response classes as `Channel/StoreClockGetList`, distinguished only by the `Command` string. | `ClassifyId`, `Flag` (reused `MyClockStoreClockGetListRequest`) | `ClockList`: list of `ClockListItem` (reused `MyClockStoreClockGetListResponse`) | device-control | decompiled |
| `Channel/StoreTop20` | Fetch the top/most-popular 20 clock faces in the online store; reuses the same request/response classes as `Channel/StoreClockGetList` (a standalone `WifiChannelStoreTop20Request` class exists but has zero callers — dead code). | `ClassifyId`, `Flag` (reused `MyClockStoreClockGetListRequest`) | `ClockList`: list of `ClockListItem` (reused `MyClockStoreClockGetListResponse`) | device-control | decompiled |
| `Channel/UploadClockConfig` | Upload a user-customized clock-face configuration (name, description, per-element setting list) to the cloud for saving/sharing. | `ClockId`, `Explain`, `ItemList` (list of `SettingListItem`), `Name` (`WifiChannelUploadClockConfigRequest`) | none (generic `BaseResponseJson`, RC only) | device-control | decompiled |

## Unknown / no signal

None. All 50 commands in this batch resolved to a `decompiled` confidence level — every one has a
matching request and/or response class in the decompiled APK, or a confirmed caller establishing it
uses a generic base request/RC-only response.
